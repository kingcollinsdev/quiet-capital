{
  "name": "gunnar-schuster-website",
  "version": "1.0.0",
  "private": true,
  "description": "Gunnar Schuster website redesign review build",
  "scripts": {
    "dev": "npx serve . -l 8765",
    "check": "node --check app.js && node --check tool-suite.js"
  }
}
